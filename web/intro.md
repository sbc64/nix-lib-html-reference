# Nix `lib` & `builtins` reference

This page is an offline-friendly HTML rendering of the Nixpkgs library
functions and the Nix language builtins, generated directly from the
upstream `nixdoc` output and `nix __dump-builtins`.

Pick a category from the left sidebar.

Sources:

- Lib docstrings → `nixdoc` over `nixpkgs/lib/<category>.nix`
- Builtins → `nix __dump-builtins`
- Theme → [jez/pandoc-markdown-css-theme](https://github.com/jez/pandoc-markdown-css-theme)
- Navigation → [htmx](https://htmx.org/)

Generated against `nixos-26.05`.
